{
  "name": "sqlite-to-csv",
  "version": "1.0.0",
  "description": "A node module to convert sqlite database into csv file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "fs": "0.0.1-security",
    "sqlite3": "^4.0.0"
  }
}
